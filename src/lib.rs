enum Piece {
    Pawn,
    Knight,
    Bishop,
    Rook,
    Queen,
    King
}

struct Board {
    pieces: 
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
